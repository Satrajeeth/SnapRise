#Fast API User Setup Instructions 

python3 -m venv venv
source venv/bin/activate

pip install -r examples/sqlalchemy/requirements.txt

//Add only psotgres and remove sqlite-only driver 
pip uninstall -y aiosqlite
pip install asyncpg

docker run --name fapi-users-pg -e POSTGRES_USER=app -e POSTGRES_PASSWORD=app -e POSTGRES_DB=appdb -p 5432:5432 -d postgres:16

Edit examples/sqlalchemy/app/db.py:
      - Replace:
          - DATABASE_URL = "sqlite+aiosqlite:///./test.db"
      - With:
          - DATABASE_URL = "postgresql+asyncpg://app:app@localhost:5432/appdb"
  - Keep create_async_engine(...), async_sessionmaker(...), and create_db_and_tables() as-is.

Set a real JWT secret for local dev

  - Edit examples/sqlalchemy/app/users.py:
      - Replace SECRET = "SECRET" with a strong random string.

Run the app

  - From examples/sqlalchemy:
      - python main.py
  - App will run at http://0.0.0.0:8000 (OpenAPI at /docs).


- Register:

  curl -X POST http://localhost:8000/auth/register \
    -H "Content-Type: application/json" \
    -d '{"email":"test@example.com","password":"StrongPass123"}'

  - Login (get JWT):

  curl -X POST http://localhost:8000/auth/jwt/login \
    -H "Content-Type: application/x-www-form-urlencoded" \
    -d "username=test@example.com&password=StrongPass123"

  - Call protected endpoint:

  curl http://localhost:8000/authenticated-route \
    -H "Authorization: Bearer <access_token>"

================================================================================================================
  # OTP Email Orchestration Framework (Standalone, Pluggable, Cost-Optimized)

  ## Summary

  Build a standalone FastAPI OTP service that:

  1. Sends OTP via multiple free email providers first.
  2. Uses weighted round-robin across eligible free providers.
  3. Automatically falls back to configured fallback providers.
  4. If all providers are unavailable, returns 202 Accepted and queues retries.
  5. Supports unlimited provider adapters (free and fallback) via a plugin-style interface.

  Service exposure will be internal-only private network, backend-to-backend access only.

  ## Public APIs / Interfaces

  ### HTTP APIs (internal)

  1. POST /v1/otp/send

  - Request: email, purpose (email_verification|password_reset), tenant_id, optional idempotency_key, optional locale
  - Response:
      - 200 when sent immediately: {request_id, status:"sent", provider_id}
      - 202 when queued due to outage/deferred retry: {request_id, status:"queued"}
      - 429 when rate-limited/backoff active

  2. POST /v1/otp/verify

  - Request: email, purpose, code, tenant_id
  - Response: 200 valid, 400 invalid/expired, 429 locked

  3. POST /v1/otp/resend

  - Same shape as send; enforces resend cooldown/backoff.

  ### Provider Adapter Contract

  Create a base interface each provider must implement:

  1. send_email_otp(payload) -> ProviderSendResult
  2. check_health() -> HealthResult
  3. classify_error(error) -> retryable | non_retryable | quota_exhausted | auth_error
  4. provider_metadata() (name, tier=free|fallback, weights, configured quotas)

  No code changes required to add providers beyond registering adapter + config.

  ## Architecture and Data Flow

  ### Core Components

  1. OtpAPI (FastAPI routes)
  2. OtpService (OTP generation, hashing, verification, attempt logic)
  3. RoutingEngine (provider eligibility + weighted selection + failover)
  4. ProviderRegistry (dynamic adapter registration)
  5. QuotaManager (configured limits + runtime counters)
  6. RetryDispatcher (queue producer/consumer)
  7. AuditLogger (events + provider attempts)

  ### Send Flow

  1. Validate request and rate limits.
  2. Generate OTP (6-digit), hash with per-record salt.
  3. Persist OTP challenge metadata.
  4. Route send:

  - Choose from healthy, quota-eligible free providers using weighted round-robin.
  - On retryable/provider failure, continue to next eligible provider.
  - If free providers unavailable/exhausted, try fallback providers (also weighted).

  5. If all fail:

  - enqueue retry job,
  - return 202 queued.

  6. Record attempt and provider telemetry.

  ### Verify Flow

  1. Fetch active challenge by (email, purpose, tenant_id).
  2. Enforce attempt/backoff policy:

  - after 1st failure: 30s lock
  - 2nd failure: 60s
  - 3rd: 120s
  - 4th: 240s
  - 5th: block challenge (max attempts reached)

  3. Compare hashed OTP.
  4. Mark verified and emit audit event.

  ## Storage Design

  ### Postgres Tables

  1. otp_challenges

  - id, tenant_id, email, purpose, otp_hash, salt, expires_at, status, attempt_count, next_allowed_at, created_at,
    verified_at

  2. otp_delivery_attempts

  - id, challenge_id, provider_id, tier, result, error_type, latency_ms, created_at

  3. provider_config

  - provider_id, tier, enabled, weight, priority, daily_limit, monthly_limit, settings_json

  4. otp_retry_jobs (or use external queue + job table)

  ### Redis Keys

  1. OTP throttles/cooldowns: otp:cooldown:{tenant}:{purpose}:{email}
  2. Provider runtime counters:

  - quota:daily:{provider}:{yyyy-mm-dd}
  - quota:monthly:{provider}:{yyyy-mm}

  3. Short-term idempotency cache: otp:idem:{tenant}:{idempotency_key}

  ## Reliability and Failover Policy

  1. Free providers used first by weighted round-robin among healthy + under-limit set.
  2. Automatic spillover to fallback group.
  3. Full outage behavior: enqueue retry + return 202.
  4. Circuit breaker per provider:

  - open on repeated failures,
  - half-open probe,
  - close on recovery.

  5. Background health checker updates eligibility.

  ## Security and Access

  1. Endpoint exposure restricted to private network only.
  2. Backend-only callers (no public client access).
  3. No end-user auth accepted at OTP service boundary.
  4. OTP never stored plaintext; hashed only.
  5. Structured audit logs for sends/verifications/failures.
  6. PII-safe logging (mask email in logs).

  ## Config Model

  Use env/config file + DB-backed provider rows:

  1. OTP_CODE_LENGTH=6
  2. OTP_TTL_SECONDS=600
  3. OTP_MAX_ATTEMPTS=5
  4. OTP_BACKOFF_SECONDS=30,60,120,240
  5. Provider registry list with unlimited entries.
  6. Queue settings (broker URL, retries, backoff strategy).

  ## Testing Plan

  ### Unit Tests

  1. OTP generation/hash/verify lifecycle.
  2. Backoff and lockout schedule.
  3. Weighted provider selection correctness.
  4. Quota exhaustion transitions free -> free -> fallback.
  5. Error classification and retryability mapping.
  6. Circuit breaker state transitions.

  ### Integration Tests

  1. Send succeeds on first free provider.
  2. First provider fails, second free succeeds.
  3. All free exhausted, fallback succeeds.
  4. All providers fail -> response 202 + queued job exists.
  5. Retry worker eventually sends after provider recovers.
  6. Verify success, invalid code, expired code, max-attempt lock.

  ### Non-Functional

  1. Load test send throughput with concurrent requests.
  2. Chaos test provider outages and recovery.
  3. Validate no plaintext OTP leakage in logs/DB.

  ## Rollout Plan

  1. Phase 1: one free provider + one fallback in production shadow mode.
  2. Phase 2: enable weighted multi-free routing.
  3. Phase 3: enable retry queue and circuit breakers.
  4. Phase 4: add provider onboarding playbook/template.

  ## Assumptions and Defaults

  1. Service remains internal-only in a trusted private network.
  2. Caller identity is controlled by network boundary and backend deployment controls.
  3. OTP use-cases in v1 are only email_verification and password_reset.
  4. Provider quotas are configured by ops and tracked via runtime counters (not provider usage APIs).
  5. Unlimited number of free/fallback providers supported via adapter registration.
  6. Full provider outage does not hard-fail request; it queues and returns 202.

================================================================================================================

This is a solid production-grade design 👍
Since you’re building this for a serious backend system (and you’re already working with FastAPI + Postgres in Docker), I’ll give you a clean execution roadmap in real engineering order, not theoretical.

⸻

🚀 STEP-BY-STEP EXECUTION PLAN

(Production-Grade OTP Email Orchestration Service)

⸻

🔵 PHASE 0 — Foundation Setup (Infra First)

Before writing OTP logic, set up the base stack properly.

Step 0.1 — Create Dedicated Service

Create a standalone repo:

otp-service/
 ├── app/
 ├── tests/
 ├── alembic/
 ├── docker/
 ├── requirements.txt
 └── docker-compose.yml

This service should:
	•	Have its own Postgres schema or database
	•	Have its own Redis
	•	Be internal-only (no public ingress)

⸻

Step 0.2 — Dockerized Production Setup

Use:
	•	FastAPI
	•	PostgreSQL
	•	Redis
	•	Celery or RQ (for retry dispatcher)
	•	Uvicorn + Gunicorn
	•	Alembic for migrations

docker-compose services:
	•	otp_api
	•	otp_worker
	•	postgres
	•	redis

This mirrors real production microservice separation.

⸻

🔵 PHASE 1 — Database & Schema Layer

Step 1.1 — Create Core Tables

Implement:

otp_challenges

otp_delivery_attempts

provider_config

otp_retry_jobs (if not using pure broker queue)

Use:
	•	UUID primary keys
	•	Indexed (tenant_id, email, purpose)
	•	Index on expires_at
	•	Partial index on active challenges

⸻

Step 1.2 — Create SQLAlchemy Models

Keep domain layer clean:

app/db/models/
    otp_challenge.py
    otp_delivery_attempt.py
    provider_config.py

Use:
	•	UTC timestamps
	•	Enum for purpose
	•	Enum for status

⸻

🔵 PHASE 2 — Core OTP Engine (Business Logic First)

Do NOT integrate providers yet.

⸻

Step 2.1 — OTP Generation Module

Create:

app/core/otp_generator.py

	•	Generate 6-digit numeric OTP
	•	Generate random salt (16 bytes)
	•	Hash: SHA256(otp + salt)
	•	Never store raw OTP
	•	Return: (plain_otp, hash, salt)

⸻

Step 2.2 — OTP Verification Module

Implement:
	•	Expiry check
	•	Hash comparison (constant-time compare)
	•	Attempt counting
	•	Backoff schedule:

attempt 1 → +30s
attempt 2 → +60s
attempt 3 → +120s
attempt 4 → +240s
attempt 5 → lock

Store:
	•	next_allowed_at
	•	attempt_count
	•	status transitions

⸻

Step 2.3 — Unit Test OTP Lifecycle

Before anything else, write tests for:
	•	Hash integrity
	•	Expiry logic
	•	Backoff schedule
	•	Lock after 5 attempts

This ensures your foundation is correct.

⸻

🔵 PHASE 3 — Provider Plugin Architecture

Now build the extensibility layer.

⸻

Step 3.1 — Define Provider Base Interface

app/providers/base.py

Define abstract class:

class BaseEmailProvider(ABC):
    async def send_email_otp(self, payload) -> ProviderSendResult
    async def check_health(self) -> HealthResult
    def classify_error(self, error) -> ErrorType
    def metadata(self) -> ProviderMetadata

No business logic inside providers.

⸻

Step 3.2 — Create Provider Registry

app/providers/registry.py

Responsibilities:
	•	Auto-register providers
	•	Load provider_config from DB
	•	Return active providers grouped by tier

Providers are injected via config — no hardcoding.

⸻

🔵 PHASE 4 — Routing Engine (Weighted Orchestration)

This is your core orchestration brain.

⸻

Step 4.1 — Weighted Round Robin Engine

Implement:

app/routing/weighted_router.py

Algorithm:
	1.	Filter:
	•	enabled
	•	healthy
	•	under quota
	•	circuit not open
	2.	Sort by tier: free first
	3.	Apply weighted selection
	4.	On failure:
	•	classify error
	•	if retryable → try next
	•	if quota_exhausted → mark temporarily exhausted
	•	if auth_error → open circuit

⸻

Step 4.2 — Quota Manager (Redis-backed)

Track:
	•	daily counters
	•	monthly counters

Keys:

quota:daily:{provider}:{date}
quota:monthly:{provider}:{month}

Atomic increments using Redis INCR.

⸻

Step 4.3 — Circuit Breaker

For each provider:

States:
	•	CLOSED
	•	OPEN
	•	HALF_OPEN

Store state in Redis:

circuit:{provider}

Rules:
	•	Open after X consecutive failures
	•	Half-open after timeout
	•	Close after successful probe

⸻

🔵 PHASE 5 — OtpService (Orchestration Layer)

Create:

app/services/otp_service.py

This coordinates:
	1.	Generate OTP
	2.	Persist challenge
	3.	Call RoutingEngine
	4.	Persist attempt record
	5.	Decide 200 or 202

This layer does NOT know specific providers.

⸻

🔵 PHASE 6 — Retry Dispatcher

Use Celery or RQ.

⸻

Step 6.1 — Retry Worker

If all providers fail:
	•	enqueue job
	•	exponential retry
	•	max retry count

Job:

retry_send_otp(challenge_id)

Worker re-runs routing logic.

⸻

Step 6.2 — Retry Backoff Strategy

Example:

retry 1 → 1 min
retry 2 → 5 min
retry 3 → 15 min
retry 4 → 30 min
retry 5 → dead-letter


⸻

🔵 PHASE 7 — API Layer (FastAPI)

Now expose routes.

⸻

Step 7.1 — POST /v1/otp/send

Flow:
	1.	Rate limit check (Redis)
	2.	Idempotency check
	3.	Call OtpService.send()
	4.	Return:
	•	200 sent
	•	202 queued
	•	429 throttled

⸻

Step 7.2 — POST /v1/otp/verify

Flow:
	1.	Fetch active challenge
	2.	Call verification logic
	3.	Return:
	•	200 valid
	•	400 invalid
	•	429 locked

⸻

Step 7.3 — POST /v1/otp/resend
	•	Check cooldown key
	•	Reuse challenge or create new one based on expiry policy
	•	Call routing engine again

⸻

🔵 PHASE 8 — Security Hardening
	1.	Mask email in logs

s***@gmail.com


	2.	Structured logging (JSON logs)
	3.	No OTP in logs ever
	4.	Enforce private network access only
	5.	Add service-to-service authentication (mTLS or internal token)

⸻

🔵 PHASE 9 — Observability

Add:
	•	Prometheus metrics:
	•	otp_send_success_total
	•	otp_send_failure_total
	•	provider_latency_histogram
	•	queued_requests_total
	•	Health endpoint:

/internal/health



⸻

🔵 PHASE 10 — Testing Strategy

Unit
	•	OTP lifecycle
	•	Backoff
	•	Quota transitions
	•	Circuit state transitions
	•	Weighted distribution fairness

Integration

Simulate:
	•	Free1 fail → Free2 succeed
	•	Free exhausted → fallback succeed
	•	All fail → 202 queued

Chaos

Simulate:
	•	Provider timeout
	•	Redis outage
	•	Postgres restart

⸻

🔵 PHASE 11 — Rollout Strategy

Phase 1

1 free provider
1 fallback
Circuit disabled

Phase 2

Enable weighted routing

Phase 3

Enable retry queue

Phase 4

Add dynamic provider onboarding template

⸻

🔥 Final Implementation Order (Do This Exactly)
	1.	Docker infra
	2.	DB schema
	3.	OTP generator + verification logic
	4.	Unit tests
	5.	Provider interface
	6.	Routing engine (without real providers)
	7.	Add 1 mock provider
	8.	Add quota manager
	9.	Add circuit breaker
	10.	Add retry worker
	11.	Add real provider adapters
	12.	Add metrics
	13.	Load testing

⸻

🧠 Engineering Advice (Important)

Because you’re building production-grade:
	•	Keep OTP service fully stateless except DB + Redis
	•	Never embed provider logic in service layer
	•	Make routing engine pure and testable
	•	Always treat provider APIs as unreliable
	•	Assume providers will fail in production

⸻

If you want next, I can give you:
	•	📁 Exact folder structure blueprint
	•	🧠 Weighted round robin algorithm implementation logic
	•	🔐 OTP hashing best-practice implementation
	•	🧪 Test case matrix
	•	🏗 Architecture diagram explanation

Tell me which one you want next.



=============================================================================

Plan: OTP Service Foundation Setup
TL;DR: Create a standalone, fully containerized OTP service with PostgreSQL, Redis, and Celery+RabbitMQ. The DB schema skeleton exists but needs conversion to Alembic migrations. We'll scaffold Dockerfiles, docker-compose.yml, and initial project structure for development.

Key Decisions Made
Task Queue: Celery + RabbitMQ (enterprise production pattern)
Database: New dedicated PostgreSQL database with separate schema for OTP
Migrations: Alembic (SQLAlchemy migrations tool)
Initial Providers: 2 free + 1-2 fallback (scaffolded, not fully implemented yet)
DB Schema: Your SnapUser_table.sql is 98% correct but needs: (1) convert to Alembic, (2) fix typo settings_jsom → settings_json, (3) add proper indexes, (4) add UUID extension
Steps
1. Fix and Convert Database Schema to Alembic

Create alembic/ folder structure with env.py, script.py.mako
Convert SnapUser_table.sql → Alembic migration file alembic/versions/001_initial_schema.py
Add fixes: fix settings_jsom typo, add indexes on (tenant_id, email, purpose), expires_at, (challenge_id), and PostgreSQL UUID extension
Create alembic.ini with OTP database connection string
2. Create Root Project Files

requirements.txt: FastAPI, SQLAlchemy, asyncpg, alembic, celery, redis, uvicorn, gunicorn, pydantic, python-dotenv
.env.example: Template for DATABASE_URL, REDIS_URL, CELERY_BROKER_URL, CELERY_RESULT_BACKEND, OTP_SECRET_KEY, DEBUG
pyproject.toml: Project metadata, black, isort, pytest config (match fastapi-users pattern)
Dockerfile: Multi-stage build for otp_api service
docker-compose.yml: Services for otp_api, otp_worker, postgres, redis, rabbitmq
3. Scaffold Core App Structure

app/init.py: Empty init
app/main.py: FastAPI app factory, startup/shutdown events for DB + Redis connectivity checks
app/config.py: Pydantic settings loader (env vars, validation)
app/db/base.py: SQLAlchemy engine, session factory, async context managers
app/db/init.py: Export engine, Session
Update app/models/ with SQLAlchemy ORM classes: OtpChallenge, OtpDeliveryAttempt, OtpRetryJob, ProviderConfig
4. Create Celery Configuration

app/celery_app.py: Celery instance, task autodiscover, broker/backend config
app/tasks/init.py: Task module
app/tasks/retry_otp.py: Skeleton task retry_send_otp(challenge_id) (stub for Phase 6)
5. Create API Structure Scaffolding

app/api/init.py: Include routers
app/api/v1/ folder with routers (stubs): otp.py (Phase 7 will implement)
app/api/health.py: /internal/health endpoint (readiness for DB, Redis, RabbitMQ)
6. Create Docker Infrastructure

docker/Dockerfile: API and worker entrypoints
docker/docker-compose.yml with:
otp_api: Uvicorn on port 8001
otp_worker: Celery worker (celery -A app.celery_app worker)
postgres: OTP database, port 5432
redis: Cache + Celery backend, port 6379
rabbitmq: Celery broker, ports 5672, 15672 (admin)
docker/.env: Dev environment variables
7. Create Test Structure

tests/init.py
tests/conftest.py: Pytest fixtures for async DB session, mocked Redis, mocked Celery (stubs for now)
tests/test_health.py: Health endpoint test
8. Documentation & Helper Scripts

README.md: Local dev setup, Docker commands, architecture overview
scripts/reset_db.sh: Alembic upgrade + drop tables (dev only)
scripts/seed_provider_config.py: Insert 2-3 stub providers into provider_config table (Phase 1 output)
Verification
After completing these steps, verify:

docker-compose up successfully starts all 4 services
OTP database tables created via Alembic: python -m alembic upgrade head
Health endpoint returns 200: curl http://localhost:8001/internal/health
Can connect to Redis: redis-cli -p 6379 ping → PONG
Can verify Celery sees the app: celery -A app.celery_app inspect version
Pytest runs without errors (fixtures mock services): pytest tests/
Files to Create/Modify
Create (new files):

requirements.txt
.env.example
pyproject.toml
Dockerfile
docker-compose.yml
alembic/env.py
alembic/script.py.mako
alembic/versions/001_initial_schema.py
alembic.ini
app/main.py
app/config.py
app/db/base.py
app/celery_app.py
app/tasks/retry_otp.py
app/api/health.py
tests/conftest.py
tests/test_health.py
Modify/Refactor:

app/db/SnapUser_table.sql → convert to Alembic (move content to alembic/versions/001_initial_schema.py)
app/models/ → add SQLAlchemy ORM models
Does this plan align with your vision? Any adjustments needed before I proceed with implementation?

Claude Haiku 4.5 • 0.33x